# Archivos ignorados para Hodei Verified Permissions

# Rust
# Directorios de artefactos de compilación de Rust (en cualquier parte del proyecto)
target/

# Archivos de respaldo generados por el editor (ej. al guardar)
**/*.rs.bk
# Información de depuración de Windows
*.pdb
# Archivos de información de compilación de Rust
.rustc_info.json

# Dependencias
# Cargo.lock debe ser rastreado para aplicaciones para asegurar compilaciones reproducibles.
# Solo se ignoraría si este fuera un proyecto de librería.
# Cargo.lock

# Archivos del sistema operativo
.DS_Store
Thumbs.db

# IDEs y Editores
.idea/
.vscode/
*.swp
*.swo
*~
.project
.classpath
.settings/

# Archivos de logs y temporales
*.log
*.tmp
*.bak
*.orig
*.rej

# Archivos de base de datos local
*.db
*.db-shm
*.db-wal

# Next.js / Node.js (para el subproyecto web-nextjs)
**/.next/
**/node_modules/
# package-lock.json debe ser rastreado para aplicaciones para asegurar compilaciones reproducibles.
# Solo se ignoraría si este fuera un proyecto de librería.
# web-nextjs/package-lock.json

# Archivos generados o temporales específicos del proyecto
.windsurf/

# ficheros de bloqueo de dependencias
Cargo.lock
**/Cargo.lock
package-lock.json
**/package-lock.json

web-nextjs/test-results
web-nextjs/playwright-report/
.kilocode/mcp.json

# Archivos de prueba/E2E y scripts de desarrollo locales (no productivos)
/dev.sh
/e2e/
/postman/VerifiedPermissions.postman_collection.json
/scripts/dev-start-managed.sh
/scripts/e2e-test.sh
